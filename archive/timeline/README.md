# Timeline Archive

Store chronological records here.

Use `TIMELINE_SCHEMA.md` and `templates/timeline-template.md`.

Recommended files:
- one file per year when enough detail exists
- one file per range when a period is still broad
- separate unplaced-memory sections when chronology is uncertain

Every major incident should link to its incident record.

